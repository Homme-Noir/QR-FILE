import SwiftUI

// TODO: display TransferRecord + ScanRecord from shared KMP storage module
struct HistoryView: View {
    var body: some View {
        VStack {
            Text("History")
                .font(.title2)
            Text("Coming in Phase 1")
                .foregroundStyle(.secondary)
        }
    }
}
