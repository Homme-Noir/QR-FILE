import SwiftUI

// TODO: theme toggle (light/dark/system), app lock (FaceID/TouchID), hidden folders
struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Settings")
                .font(.title2)
            Text("Coming in Phase 1")
                .foregroundStyle(.secondary)
        }
    }
}
